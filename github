echo "# MealTime3D" >> README.md
git init
git add .
git commit -m "raycast"
git branch -M main
git remote add origin https://github.com/MalwarePig/MealTime3D.git
git push -u origin main

…or push an existing repository from the command line

git remote add origin https://github.com/MalwarePig/MealTime3D.git
git branch -M main
git push -u origin main